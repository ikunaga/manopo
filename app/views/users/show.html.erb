<h4>ユーザー詳細</h4>

	<div style="width: 100%;">
		<%= attachment_image_tag @user, :image, :fill, 500, 300, format: 'jpeg' %>
	</div>
<%= @user.email %>
<div>
<%= flash[:success] %>
</div>
<%= render "relationships/follow_button", user: @user %>


<% if @user == current_user %>
<%= link_to "編集" ,edit_user_path(@user.id) %>
<% end %>

